<style>
.form-control {
  width: 80%;
}
.complain-btn {
  margin-bottom: 10px;
}
.btn-login-sub {
  margin-left: 30%;
  width: 45%;
  height: 47px;
}
.user-avatar img{
  height: 100px;
  width: 100px;
  margin-bottom: 24px;
}
.btn-upload {
  margin-left: 135px;
  margin-top: -86px;
  margin-bottom: 48px;
  width: 36%;
}
</style>
<% if(req.session.authenticated) { %>

  <table class="table">
    <thead>
        <tr>
          <th>Account Details</th>
        </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <div class="alert alert-warning" id="password-tip">
            Remember, Clicking on <strong>delete</strong> button will delete your account beyond recovery.
          </div>
          <button type="submit" class="btn btn-default complain-btn btn-danger" id="delete-account">Delete Acccount</button>
          <div class="alert alert-success" id="success-msg" style="display:none">
            <strong>Aww Snap!</strong> We are sad to let you go. But you can always join us back.
          </div>
          <div class="alert alert-danger" id="error-msg" style="display:none">
            <strong>OOPS!</strong> We are facing some technical difficulties. Please try again after some time.
          </div>
        </td>
      </tr>
    </tbody>
  </table>
  <div class="divider-border"></div>

    <!-- <div class="bug-report">
      <h3>Found a Bug?</h3>
        <h4>Mail us  <span style="color:red">bug@softard.com</span></h4>
        <br><br><br>
        <p>If your query is not yet resolved mail us : <span style="color:blue">help@softard.com.</span>We will make sure someone gets back to you in 24 hours.</p>
    </div> -->

<% } else { %>
  <div class="alert alert-danger" id="error-msg">
    <strong>OOPS! </strong>Your session expired. Please login again.
  </div>
  <button type="button" class="btn btn-primary btn-login-sub" data-toggle = "modal" data-target="#loginModal">Login</button>
  <% } %>
